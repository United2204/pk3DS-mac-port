const $=id=>document.getElementById(id);let game,table,original;
async function post(url,body={}){const response=await fetch(url,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)}),data=await response.json();if(!response.ok)throw Error(data.error||data.detail||'Operación fallida');return data}
function changed(){return !!table&&JSON.stringify(table)!==JSON.stringify(original)}function ui(){$('summary').textContent=changed()?'Listas modificadas':'Sin cambios para exportar';$('export').disabled=!game?.titleId||!changed()}
function moveSelect(group,index,value){const select=document.createElement('select');select.dataset.group=group;select.dataset.index=index;for(const move of table.moves){const option=new Option(`${move.id} · ${move.name}`,move.id);option.selected=move.id===value;select.add(option)}return select}
function render(){const box=$('groups');box.replaceChildren();table.groups.forEach((group,groupIndex)=>{const section=document.createElement('section');section.className='tutor6-group';const title=document.createElement('h2');title.textContent=group.name;const list=document.createElement('div');list.className='tutor6-list';group.moves.forEach((move,index)=>{const row=document.createElement('label');row.className='tutor6-row';row.append(`Movimiento ${String(index+1).padStart(2,'0')}`,moveSelect(groupIndex,index,move));list.append(row)});section.append(title,list);box.append(section)});$('warning').textContent=table.warning;ui()}
async function load(){try{game=await post('/api/workspace/inspect',{workspacePath:$('workspace').value});$('open').disabled=false;$('editor').classList.remove('is-disabled');$('status').textContent=`${game.gameVersion} cargado.`}catch(error){$('status').textContent=error.message}ui()}
async function open(){try{table=await post('/api/editors/tutors/gen6/table',{workspacePath:$('workspace').value,language:1});original=structuredClone(table);render()}catch(error){$('result').textContent=error.message}}
$('browse').onclick=async()=>{try{$('workspace').value=(await post('/api/workspace/pick')).path;await load()}catch(error){$('status').textContent=error.message}};$('load').onclick=load;$('open').onclick=open;$('groups').onchange=event=>{const group=event.target.dataset.group,index=event.target.dataset.index;if(group===undefined||index===undefined)return;table.groups[+group].moves[+index]=+event.target.value;ui()};$('export').onclick=async()=>{try{const output=await post('/api/workspace/pick-output'),result=await post('/api/editors/tutors/gen6/export',{workspacePath:$('workspace').value,outputDirectory:output.path,titleId:game.titleId,groups:table.groups,language:1});original=structuredClone(table);$('result').textContent=`Listo. ZIP: ${result.zipPath}`;ui()}catch(error){$('result').textContent=error.message}};ui();
