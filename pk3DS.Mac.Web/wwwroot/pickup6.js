const $ = id => document.getElementById(id);
let game, table, original;
async function post(url, body = {}) { const response = await fetch(url, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(body) }); const data = await response.json(); if (!response.ok) throw Error(data.error || data.detail || 'Operación fallida'); return data; }
function changed() { return !!table && JSON.stringify(table) !== JSON.stringify(original); }
function ui() { $('summary').textContent = changed() ? 'Listas modificadas' : 'Sin cambios para exportar'; $('export').disabled = !game?.titleId || !changed(); }
function itemSelect(side, index, value) { const select = document.createElement('select'); select.dataset.side = side; select.dataset.index = index; for (const item of table.items) { const option = new Option(`${item.id} · ${item.name}`, item.id); option.selected = item.id === value; select.add(option); } return select; }
function renderList(id, side, values) { const box = $(id); box.replaceChildren(); values.forEach((value, index) => { const row = document.createElement('label'); row.className = 'tm-row'; row.append(`${side === 'common' ? 'Común' : 'Raro'} ${index + 1}`, itemSelect(side, index, value)); box.append(row); }); }
function render() { renderList('common', 'common', table.common); renderList('rare', 'rare', table.rare); $('warning').textContent = table.warning; ui(); }
async function load() { try { game = await post('/api/workspace/inspect', { workspacePath: $('workspace').value }); $('open').disabled = false; $('editor').classList.remove('is-disabled'); $('status').textContent = `${game.gameVersion} cargado.`; } catch (error) { $('status').textContent = error.message; } ui(); }
async function open() { try { table = await post('/api/editors/pickup/gen6/table', { workspacePath: $('workspace').value, language: 1 }); original = structuredClone(table); render(); } catch (error) { $('result').textContent = error.message; } }
$('browse').onclick = async () => { try { $('workspace').value = (await post('/api/workspace/pick')).path; await load(); } catch (error) { $('status').textContent = error.message; } }; $('load').onclick = load; $('open').onclick = open;
$('editor').onchange = event => { const side = event.target.dataset.side; if (!side) return; table[side][+event.target.dataset.index] = +event.target.value; ui(); };
$('export').onclick = async () => { try { const output = await post('/api/workspace/pick-output'); const result = await post('/api/editors/pickup/gen6/export', { workspacePath: $('workspace').value, outputDirectory: output.path, titleId: game.titleId, common: table.common, rare: table.rare, language: 1 }); original = structuredClone(table); $('result').textContent = `Listo. ZIP: ${result.zipPath}`; ui(); } catch (error) { $('result').textContent = error.message; } };
ui();
